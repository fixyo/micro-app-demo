import { defineComponent } from 'vue'

const BigScreenHeader = defineComponent({
  // props: {},

  setup(props, ctx) {
    return () => <div>big screen header</div>
  }
})

export default BigScreenHeader
